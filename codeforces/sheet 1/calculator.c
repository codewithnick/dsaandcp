#include<iostream>
#include<string.h>
#include<iomanip>
using namespace std;
int main(){
    string s1;
    cin>>s1;
    if(s1.contains('+'))
    {
        cout<<s.substr(0, s.find('+'));
        cout<<s.substr(s.find('+')+1, s.length());
    }

}



